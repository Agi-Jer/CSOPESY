{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Compile and Run Emulator",
            "type": "mock",
            "request": "launch",
            "program": "${workspaceFolder}/main",
            "stopOnEntry": false,
            "preLaunchTask": "run-in-ptyxis"
        }
    ]
}